"""Shared helpers extracted from the git-metadata-extractor v2 package.

canonicalization/ — pure IRI builders + string normalisation (was src/v2/canonicalization)
cache.py         — SQLite-backed ProviderCache (was src/v2/ingest/cache.py)
"""
